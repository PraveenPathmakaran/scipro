import 'package:flutter/material.dart';

class ViewCoursesContainer extends StatelessWidget {
  const ViewCoursesContainer({super.key});

  @override
  Widget build(BuildContext context) {
    return AlertDialog();
  }
}